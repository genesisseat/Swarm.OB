import React from "react";

export function SwarmView(): JSX.Element {
  return (
    <section className="view-card">
      <h2>Swarm</h2>
      <p>Create a topic, configure the agent roster, run the debate, and capture a synthesis.</p>
      <button className="primary-button">Start debate</button>
    </section>
  );
}
